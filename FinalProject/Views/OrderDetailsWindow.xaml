<Window x:Class="FinalProject.Views.OrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        d:DesignHeight="600" d:DesignWidth="800"
        Title="Order Details"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinWidth="600" MinHeight="400">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16" Padding="16">
            <StackPanel>
                <TextBlock Text="Order Details" Style="{DynamicResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,8"/>
                <TextBlock x:Name="OrderIdText" Style="{DynamicResource MaterialDesignBody1TextBlock}" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Order Information -->
        <materialDesign:Card Grid.Row="1" Margin="0,0,0,16" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,16,0">
                    <TextBlock Text="Customer Information" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,8"/>
                    <TextBlock x:Name="CustomerNameText" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                    <TextBlock x:Name="CustomerEmailText" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                    <TextBlock x:Name="CustomerPhoneText" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0">
                    <TextBlock Text="Order Information" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,8"/>
                    <TextBlock x:Name="OrderDateText" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                    <TextBlock x:Name="TableText" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                    <TextBlock x:Name="StatusText" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="0,16,0,0">
                    <TextBlock Text="Staff Information" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,8"/>
                    <TextBlock x:Name="StaffNameText" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Order Items -->
        <materialDesign:Card Grid.Row="2" Margin="0,0,0,16" Padding="16">
            <StackPanel>
                <TextBlock Text="Order Items" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,16"/>
                <DataGrid x:Name="OrderItemsDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Style="{DynamicResource MaterialDesignDataGrid}"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeColumns="True"
                          CanUserResizeRows="False"
                          CanUserSortColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Item" Binding="{Binding Item.ItemName}" Width="*"/>
                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                        <DataGridTextColumn Header="Unit Price" Binding="{Binding ItemPrice, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </materialDesign:Card>

        <!-- Order Summary -->
        <materialDesign:Card Grid.Row="3" Margin="0,0,0,16" Padding="16">
            <StackPanel>
                <TextBlock Text="Order Summary" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,16"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Subtotal:" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                    <TextBlock x:Name="SubtotalText" Grid.Row="0" Grid.Column="1" Style="{DynamicResource MaterialDesignBody1TextBlock}" HorizontalAlignment="Right"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Discount:" Style="{DynamicResource MaterialDesignBody1TextBlock}"/>
                    <TextBlock x:Name="DiscountText" Grid.Row="1" Grid.Column="1" Style="{DynamicResource MaterialDesignBody1TextBlock}" HorizontalAlignment="Right"/>

                    <Separator Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,8,0,8"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Final Amount:" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" FontWeight="Bold"/>
                    <TextBlock x:Name="FinalAmountText" Grid.Row="3" Grid.Column="1" Style="{DynamicResource MaterialDesignSubtitle1TextBlock}" FontWeight="Bold" HorizontalAlignment="Right"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <!-- Close Button -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" 
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Style="{DynamicResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0"/>
        </StackPanel>
    </Grid>
</Window> 