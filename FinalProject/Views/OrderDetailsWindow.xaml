<Window x:Class="FinalProject.Views.OrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Order Details" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Order Info -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="Order ID: " FontWeight="Bold"/>
            <TextBlock Text="{Binding OrderId}" Margin="0,0,16,0"/>
            <TextBlock Text="Date: " FontWeight="Bold"/>
            <TextBlock Text="{Binding OrderDate}" Margin="0,0,16,0"/>
            <TextBlock Text="Status: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Status}" Margin="0,0,16,0"/>
        </StackPanel>
        <!-- Customer, Table, Staff Info -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Customer: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Customer.FullName}" Margin="0,0,16,0"/>
            <TextBlock Text="Table: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Table.TableName}" Margin="0,0,16,0"/>
            <TextBlock Text="Staff: " FontWeight="Bold"/>
            <TextBlock Text="{Binding Account.FullName}" Margin="0,0,16,0"/>
        </StackPanel>
        <!-- Order Items -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding OrderItems}"
                  AutoGenerateColumns="False"
                  Margin="0,0,0,8"
                  IsReadOnly="True"
                  Style="{DynamicResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item" Binding="{Binding Item.ItemName}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding ItemPrice}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <!-- Order Summary and Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock Text="Total: " FontWeight="Bold" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding TotalAmount}" Margin="0,0,16,0"/>
            <TextBlock Text="Discount: " FontWeight="Bold" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding Discount}" Margin="0,0,16,0"/>
            <TextBlock Text="Final: " FontWeight="Bold" Margin="0,0,8,0"/>
            <TextBlock Text="{Binding FinalAmount}" Margin="0,0,16,0"/>
            <Button Content="Close" Click="Close_Click" Style="{DynamicResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
    </Grid>
</Window> 