<Window x:Class="FinalProject.Views.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Login"
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="22*"/>
            <ColumnDefinition Width="23*"/>
        </Grid.ColumnDefinitions>

        <!-- Branding Section -->
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Height="356" Width="470">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,0">
                <Image Source="pack://application:,,,/FinalProject;component/Images/logo.png" Width="255" Height="235" Margin="0,0,0,24"/>
                <TextBlock Text="Restaurant Management System"
                           FontSize="27"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           Margin="0,0,0,12" Width="401"/>
                <TextBlock Text="Welcome to our restaurant management"
                           Style="{DynamicResource MaterialDesignBody1TextBlock}"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           MaxWidth="320"
                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
            </StackPanel>
        </Grid>

        <!-- Login Section -->
        <Grid Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Height="372" Width="380">
            <materialDesign:Card Padding="48" MinWidth="380" MaxWidth="420" Margin="0,-48,0,-76">
                <StackPanel>
                    <TextBlock Text="Sign In"
                               Style="{DynamicResource MaterialDesignHeadline5TextBlock}"
                               Margin="0,0,0,32"
                               HorizontalAlignment="Center"/>
                    <TextBox x:Name="UsernameBox"
                             materialDesign:HintAssist.Hint="Username"
                             Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                             Margin="0,8,0,8"
                             MinWidth="280"
                             FontSize="15"/>
                    <PasswordBox x:Name="PasswordBox"
                                 materialDesign:HintAssist.Hint="Password"
                                 Style="{DynamicResource MaterialDesignOutlinedPasswordBox}"
                                 Margin="0,8,0,8"
                                 MinWidth="280"
                                 FontSize="15"/>
                    <Button Content="Login"
                            Style="{DynamicResource MaterialDesignRaisedButton}"
                            Background="#4CAF50"
                            Foreground="White"
                            Margin="0,32,0,0"
                            Height="48"
                            FontSize="15"
                            Click="LoginButton_Click"
                            HorizontalAlignment="Stretch"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window> 